name: Android CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  reusable-workflow:
    uses: GMZwinge/hello-world-composite-action/.github/workflows/workflow.yml@v4

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - uses: GMZwinge/hello-world-composite-action/.github/actions/action@v4
    - uses: GMZwinge/hello-world-composite-action/.github/actions/action@v4
